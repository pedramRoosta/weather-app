name: weather_app
description: "a nice weather app that shows the weather of the city around the world"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.6.7
  go_router: ^13.1.0
  cupertino_icons: ^1.0.2
  dio: ^5.4.0
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
  freezed: ^2.4.7
  freezed_annotation: ^2.4.1
  flutter_svg: ^2.0.9
  json_annotation: ^4.8.1
  mocktail: ^1.0.2
  intl: ^0.19.0
  geolocator: ^11.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.8
  json_serializable: ^6.7.1
  golden_toolkit: ^0.15.0
  bloc_test: ^9.0.2
#   flutter_launcher_icons: ^0.13.1
# flutter_launcher_icons:
#   android: "launcher_icon"
#   ios: true
#   remove_alpha_ios: true
#   image_path: "assets/icon/logo.png"
#   min_sdk_android: 21 

flutter:
  uses-material-design: true

  assets:
    - assets/images/